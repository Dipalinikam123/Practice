/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.annihil.us','images.unsplash.com'], // Allow the domain for external images
      },
};

export default nextConfig;
